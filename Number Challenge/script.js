const x = Math.floor(Math.random() * 100 + 1);

const y = Math.floor(((Math.random()*100 + 1)/ 2) + 1);

const sum = x + y;
const diff = x - y;
const pro = x * y;
const rem = x % y;
const div = x / y;

console.log(sum,diff,pro,rem,div);